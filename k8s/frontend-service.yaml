apiVersion: v1
kind: Service
metadata:
  name: weather-frontend
  labels:
    app: weather-frontend
spec:
  type: ClusterIP
  selector:
    app: weather-frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000