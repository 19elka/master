spring:
  application:
    name: book-service

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

  kafka:
    bootstrap-servers: host.minikube.internal:29092
    consumer:
      group-id: book-service-users

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/booksdb
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:changelog/db.changelog-master.yaml
    default-schema: public

server:
  port: 8082

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: 3600000

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

springdoc:
  swagger-ui:
    persist-authorization: true
    display-request-duration: true
    operations-sorter: method
  api-docs:
    path: /v3/api-docs
  show-actuator: true